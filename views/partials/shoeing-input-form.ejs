<%#
    This partial generates the form for each individual hoof.

    Explicitly passed in on webpage.
    @input {String} hoof This is the regular version of hoof that is passed in (Front Right).
    @input {String} hoofIdent This is the variable version of hoof (meaning no spaces, frontRight).

    Example: include('partials/shoeing-input-form', {hoof: 'Back Right', hoofIdent: "backRight"})
%>


<div class="col-sm-6 shoeing">
  <h1><%= hoof %></h1>

  <div class="form-group">
    <h2>Images</h2>
    <input type="file" class="form-control" id="images" name="<%= hoofIdent %>Images" multiple>
  </div>

  <div class="form-group">
    <label for="size">
      <h3>Shoe Size</h3>
    </label>
    <input type="number" class="form-control" id="size" name="<%= hoofIdent %>Size"
      placeholder="Enter Horse Shoe size">
  </div>

  <div class="form-group">
    <label for="notes">
      <H3>Notes:</H3>
    </label>
    <textarea class="form-control" id="notes" name="<%= hoofIdent %>Notes" rows="4"
      placeholder="Notes specific to this shoe."></textarea>
  </div>
</div>
