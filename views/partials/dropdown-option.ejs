<%#
    This creates a single option in a dropdown and determines wheather or
    not it will be the default (selected) option.

    Explicitly passed in on webpage.
    @input {Boolean} selected Determines whether or not a
    @input {String} option Determines text displayed in dropdown.

    Example: include('dropdown-option', {option: "pads", selected: "True"})
%>

<option <%= selected ? 'selected' : '' %>><%= option %></option>