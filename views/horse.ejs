<%- include('partials/head', { title: 'Horse Profile', css: 'horse' }) %>

<body>
  <%- include('partials/header-nav') %>
  <div id="page-container" class="container-fluid">
    <%# Horse Summary %>
    <div class="container form">
      <div class="dropdown" align="right">
        <button class="btn dropdown-toggle fa fa-edit" type="button"
          data-toggle="dropdown"></button>
        <ul class="dropdown-menu">
          <li><a href="/horse/<%= horse.id %>/update">Update</a></li>
          <li><a href="#">Delete (does not work yet)</a></li>
        </ul>
      </div>
      <div class="row">
        <div class="col-sm-4">
          <img alt="Horse" class="img-fluid float-left"
            src=<%= horse.image === undefined ? "../public/images/horse_profile.png" : horse.image.url %> />
        </div>
        <div class="col-sm-4">
          <table class="table table-borderless">
            <thead>
              <tr></tr>
            </thead>
            <tbody>
              <tr>
                <th scope="row">Name:</th>
                <td><%= horse.name %></td>
              </tr>
              <tr>
                <th scope="row">Gender</th>
                <td><%= horse.gender %></td>
              </tr>
              <tr>
                <th scope="row">Temperament</th>
                <td><%= horse.temperament %></td>
              </tr>
              <tr>
                <th scope="row">Discipline</th>
                <td><%= horse.discipline%></td>
              </tr>
              <tr>
                <th scope="row">Location</th>
                <td><%= horse.location %></td>
              </tr>
              <tr>
                <th scope="row">Owner</th>
                <td><%= horse.owner %></td>
              </tr>
              <tr>
                <th scope="row">Vet</th>
                <td><%= horse.vet %></td>
              </tr>
              <tr>
                <th scope="row">History</th>
                <td><%= horse.history %></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
    <%# Horse Medical Analysis %>

    <div class="container form">
      <div class="row justify-content-center">
        <div class="pad">
          <a class="btn btn-dark" data-toggle="collapse" href="#medical" role="button"
            aria-expanded="false" aria-controls="medical">
            Show Medical Analysis
          </a>
        </div>
        <div class="pad">
          <a type="button" class="btn" href="/horse/<%= horse.id %>/new-report">
            New Medical Analysis
          </a>
        </div>
      </div>
      <!--If shoeings is undefined shoeing.medicals is a ReferenceError-->
      <!-- <% if (shoeings !== undefined) { %> -->
      <div class="collapse" id="medical">
        <div class="row">
          <% if (shoeings !== undefined) { %>
          <% if (shoeings.medical !== undefined) { %>
          <div class="col-sm-4">
            <h1>Medical Analysis</h1>
          </div>
          <div class="col-sm-4">
            <h1>Date:
              <%= `${shoeings.date.getMonth() + 1}/${shoeings.date.getDate()}/${shoeings.date.getFullYear()}` %>
            </h1>
          </div>
          <div class="col-sm-4">
            <h1>Farrier: <%= shoeings.farrier %></h1>
          </div>
        </div>
        <%- include('partials/report-medical-fields', {medical: shoeings.medical, editable: false}) %>
        <% } %>
        <% } else { %>
        <div align="center">
          <H2> No medical reports have been created</H2>
        </div>
        <% } %>
        <!-- <% } %> -->
        <!--If shoeing is undefined-->
      </div>
    </div>

    <%# Horse Most Recent Shoeing %>
    <div class="container form">
      <div class="row justify-content-center">
        <div class="pad">
          <a class="btn btn-dark" data-toggle="collapse" href="#shoeing" role="button"
            aria-expanded="false" aria-controls="medical">
            Show Most Recent Shoeing Report
          </a>
        </div>
        <div class="pad">
          <a type="button" class="btn btn-dark" href="/horse/<%= horse.id %>/new-report">
            New Shoeing
          </a>
        </div>
      </div>
      <!--If shoeing is undefined shoeings.front and shoeings.back are ReferenceError-->
      <!-- <% if (shoeings !== undefined) { %> -->
      <div id="shoeing" class="collapse">
        <% if(shoeings.length > 0) { %>
        <% let mostRecentShoeing = shoeings[0]%>
        <div class="row">
          <div class="col-sm-4">
            <h2>Most Recent Shoeing</h2>
          </div>
          <div class="col-sm-4">
            <h1>Date:
              <%= `${mostRecentShoeing.date.getMonth() + 1}/${mostRecentShoeing.date.getDate()}/${mostRecentShoeing.date.getFullYear()}` %>
            </h1>
          </div>
          <div class="col-sm-4">
            <h2>Farrier: <%= shoeings.farrier %></h2>
          </div>
        </div>
        <% } %>

        <%# Shoeing Data %>
        <% if (shoeings.front.horseshoes.length !== 0) { %>
        <div class="row">
          <div class="col-sm-6 form">
            <%- include('partials/shoeing', {area: 'Front Left', hoof: shoeings.front.horseshoes[0]}) %>
          </div>

          <div class="col-sm-6 form">
            <%- include('partials/shoeing', {area: 'Front Right', hoof: shoeings.front.horseshoes[1]}) %>
          </div>
        </div>
        <% } %>

        <% if (shoeings.back.horseshoes.length !== 0) { %>
        <div class="row">
          <div class="col-sm-6 form">
            <%- include('partials/shoeing', {area: 'Back Left', hoof: shoeings.back.horseshoes[0]}) %>
          </div>

          <div class="col-sm-6 form">
            <%- include('partials/shoeing', {area: 'Back Right', hoof: shoeings.back.horseshoes[1]}) %>
          </div>
        </div>

        <% } %>
        <% if (shoeings.front.horseshoes.length === undefined && shoeings.back.horseshoes.length === undefined) { %>
        <div align="center">
          <H2> No Shoeing reports have been created</H2>
        </div>
        <% } %>
        <div>
          <% for (url of shoeings.images) { %>
          <label for="images">Other Images</label>
          <img alt="Report images" id="images" class="img-fluid float-left" src=<%= url %> />
          <% } %>
        </div>
      </div>
      <div class="accordion" id="shoeings-accordian">

        <!--Will be populated with all the shoeings done on a horse-->
        <% for (const singleShoeing of shoeings) { %>
        <%-  include('partials/shoeing-thumbnail', {singleShoeing}) %>
        <% } %>

      </div>
    </div>
  </div>
  <%- include('partials/footer') %>
</body>
